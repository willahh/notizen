{
  "name": "root",
  "private": true,
  "workspaces": [
    "frontend/**/*"
  ],

  "scripts": {
    "start": "yarn workspace @notizen/frontend-web run start",
    "build": "yarn workspace @notizen/frontend-web run build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
